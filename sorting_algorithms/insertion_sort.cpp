#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int insertion_sort(vector<int> &arr1, int length){
    for (int i = 1; i < length; i ++){
        int key = arr1[i];
        int j = i -1;
    }
}