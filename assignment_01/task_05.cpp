#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main(){
    int length;
    cin >> length;
    vector<int> arr1(length);
    vector<int> sortedArr1(length);
    vector<int> evenArr, oddArr,even_sorted, odd_sorted;
    for (int i = 0; i < length; i++){
        int elem;
        cin >> elem;
        arr1[i] = elem;
    }



// incomplete code


    return 0;
}